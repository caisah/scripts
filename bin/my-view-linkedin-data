#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
repo_root="$(cd -- "${script_dir}/.." && pwd)"
profile_activity_dir="${repo_root}/local-html/profile-activity"

exec http-server "${profile_activity_dir}" "$@"
